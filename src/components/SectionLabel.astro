---
interface Props {
  label: string;
  number?: number;
  color?: string;
}

const { label, number, color = 'text-brand-teal' } = Astro.props;
---

<div class={`font-mono text-xs ${color} tracking-widest uppercase`}>
  {label} {number && `· ${String(number).padStart(2, '0')}`}
</div>
